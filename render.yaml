services:
  - type: web
    name: ebook-store-api
    runtime: php
    buildCommand: composer install --no-dev --optimize-autoloader --no-scripts
    startCommand: php artisan serve --host=0.0.0.0 --port=$PORT
    healthCheckPath: /api/v1/ebooks